{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:unstable": "npx lerna run build --scope=UnstableService",
    "start:unstable": "npx lerna run start --scope=UnstableService",
    "build:server": "npx lerna run build --scope=Server",
    "start:server": "npx lerna run start --scope=Server",
    "build": "npx lerna run build",
    "start": "npx lerna run start"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.10.0",
    "eslint": "^8.53.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-config-standard-with-typescript": "^39.1.1",
    "eslint-plugin-import": "^2.29.0",
    "eslint-plugin-n": "^16.3.1",
    "eslint-plugin-promise": "^6.1.1",
    "lerna": "^7.4.2",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
